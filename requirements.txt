# WSR-YOLO reproducibility environment.
# Torch and torchvision are installed separately so the CUDA-specific wheel is
# not overwritten. Install them first:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
# Then run: pip install -r requirements.txt

ultralytics==8.4.50
numpy==1.26.4
opencv-python==4.10.0.84
matplotlib==3.9.2
PyYAML==6.0.2
Pillow>=10
scipy==1.14.1
gdown>=5.2
pycocotools>=2.0.10
thop==0.1.1.post2209072238
pytest>=8
