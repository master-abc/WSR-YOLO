# DWGSA-YOLO Requirements
# Note: torch / torchvision are NOT pinned here to avoid overwriting the
# CUDA-specific wheel installed via the PyTorch index. Install them first:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
# then run `pip install -r requirements.txt`.

ultralytics==8.4.50
numpy==1.26.4
opencv-python==4.10.0.84
matplotlib==3.9.2
pyyaml==6.0.2
thop==0.1.1.post2209072238

# Post-processing scripts (eval_all.py, generate_tables.py)
pandas==2.2.3
seaborn==0.13.2
scipy==1.14.1
